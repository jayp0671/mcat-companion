import { Card, CardContent, CardTitle } from "@/components/ui/card";
export default function SettingsPage() { return <Card><CardTitle>Settings</CardTitle><CardContent>Profile, export, delete account, and notification settings will live here.</CardContent></Card>; }
