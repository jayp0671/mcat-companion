import { Card, CardContent, CardTitle } from "@/components/ui/card";
export function ModePicker() { return <Card><CardTitle>Choose mode</CardTitle><CardContent>Weak topic, weak skill, and mixed practice modes will land after the mistake log and AI pipeline.</CardContent></Card>; }
